function rspec {
  bundle exec rspec "$@"
}

function rake {
  bundle exec rake "$@"
}

function rails {
  bundle exec rails "$@"
}

function clockwork {
  bundle exec clockwork "$@"
}

function sidekiq {
  bundle exec sidekiq "$@"
}
